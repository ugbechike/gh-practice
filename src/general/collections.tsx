export function Collections() {
    let arr = [1, 2, 3, 4, 5]
    let a = 5
    let b = 8

    // let me help you

    arr.map((item, index) => item)

    return 1+1
}

function addSum(a:number, b:number ){
    return a + b
}